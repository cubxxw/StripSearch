# StripSearch · Evidence Terminal 最终设计

状态：本地合成交互设计原型。`design/web/index.html` 以 Open Design 方向 A 为起点收敛，不改变 M1–M4 验收，也不代表已有采集、模型推理或后端服务。

- 概念来源：[BRIEF.md](BRIEF.md)、[concept-a.html](concept-a.html)（B 方向保留为 [concept-b.html](concept-b.html)，不再合并）
- 共享背景：仓库 [Web 设计](../../docs/web-design.md)
- 离线 DOM 测试：[qa/prototype.test.cjs](qa/prototype.test.cjs)
- 本地静态访问与测试命令：[README.md](README.md)

## 品牌与设计令牌

Evidence Terminal：问题进入，公开记录被连接，结论保持可检查。

默认跟随系统的亮 / 深色偏好，系统切换时自动更新，无需刷新或保存主题覆盖。无明确偏好时使用亮色。通过 CSS `prefers-color-scheme` 与 `color-scheme` 同时控制页面和原生表单控件；不依赖 JavaScript 初始化，首屏即使用相应主题。

| 令牌 | 亮色 | 深色 | 用途 |
| --- | --- | --- | --- |
| `--bg` | `#f7f9f6` | `#0a0a0a` | 页面、抽屉与报告基底 |
| `--surface` / `--surface-2` / `--surface-3` | `#ffffff` / `#eef3ee` / `#e4ebe5` | `#111316` / `#171a1d` / `#1d2125` | 面板、表单、消息、引用块 |
| `--fg` / `--fg-2` | `#12231a` / `#35483d` | `#edf0ed` / `#c7ccc8` | 标题正文 / 次级正文 |
| `--muted` / `--faint` | `#526459` / `#5b6c61` | `#9ba69f` / `#858e87` | 辅助文字 / 编号 |
| `--border` / `--border-strong` | `#d7e0d8` / `#7b8c80` | `#282d29` / `#404741` | 分隔 / 结构边界 |
| `--accent` / `--accent-hover` | `#006b31` / `#005527` | `#00ff41` / `#5aff7d` | 主按钮、焦点、引用 / 悬停 |
| `--accent-dark` / `--accent-on` | `#e1f0e5` / `#ffffff` | `#003b12` / `#061108` | 选中态底色 / 主按钮文字 |
| `--warning` | `#946200` | `#ffb800` | 合成样例徽标圆点 |
| `--danger` / `--danger-dark` | `#aa293a` / `#fff0f0` | `#ff6b6b` / `#351819` | 撤下、错误、待复核 |
| `--success` | `#23693b` | `#8fe6a5` | 有效状态文字 |

两套主题共用布局与证据状态。导航透明底色、网格、阴影、占位文字、焦点光晕、抽屉遮罩与危险按钮边框使用独立主题令牌，避免亮色页面残留深色组件。`--radius` 两种主题均为 `5px`。

参考：[MDN prefers-color-scheme](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/At-rules/@media/prefers-color-scheme) · [MDN color-scheme](https://developer.mozilla.org/en-US/docs/Web/CSS/Reference/Properties/color-scheme)。

排版与节奏：

- 正文 `16px / 1.65`，中文用系统无衬线回退，标题允许放大。
- 等宽元信息 `12px`：编号、日期、来源代码、状态标签；不使用浏览器小字伪装精密。
- 结构线为 1px 细网格背景与 `--border` 分隔线，网格只组织真实内容。
- 动效强度 5/10：100–240ms，仅 `transform` 与 `opacity`；`prefers-reduced-motion` 下取消位移与动画速度。
- 触控目标不小于 44×44px；焦点环使用 `--accent`。
- 单一小尺寸合成徽标「交互演示 · 合成样例」固定出现在首页、App 和报告；不重复堆叠免责声明。

## 信息架构

### 首页 · 输入优先且紧凑

- 两行标题「理解一个人，／沿着证据走。」与一句产品主张。
- 研究问题是主体：必填 `textarea` 位于标题之后；可选公开主页与研究模式收在「更多设置（可选）」的折叠中。
- 主按钮「开始研究」紧随问题字段；`640px` 及以下单列堆叠，保证标题、问题与主操作在首屏或接近首屏内可见。
- ALLOWED / EXCLUDED 边界移到研究表单之后，改为产品语言「只研究公开职业材料」，不再出现 `scope public-work-only` 之类实现术语。
- 保留响应式证据机制区：问题 → 公开记录（S1–S4 编号轨道）→ 可核查结论；首页引用点击会打开可见的来源抽屉，并同步更新旁边的事实/限制预览。

### App 视图 · 全窗口研究工作台

- `#/app` 独立视图，由表单校验成功或「打开演示 / Chat 工作台」导航进入；`#/` 返回首页。App 视图隐藏营销导航、页脚与长标题，只保留研究界面。
- 桌面：左侧研究历史、中间 Chat 与结构化报告、右侧来源检查器；工作区高度对齐视口，正文区独立滚动。
- 窄屏：单列，历史与来源进入可关闭抽屉。对话滚动区与底部 composer 分占网格行，输入区持续可见且不覆盖回答。
- 用户问题位于研究过程之前；完成后的研究过程默认折叠，可展开查看 5 个可观察步骤。正文与证据面板分别滚动。
- 结构：用户研究问题 → 紧凑的可观察活动（含 S4 无法访问与重试）→ 结构化回答（人物摘要、时间线、已支持事实、分析推断、反证与未知）→ 预设追问 composer。
- 任意追问若与预设问题不完全一致，只返回诚实的原型边界说明；匹配使用精确字符串，不做关键词猜测。

## 关键流程

1. 提交校验：`new URL` 解析公开主页（协议限 http/https 且要求非空 hostname，「https://」失败）；研究问题必填。错误就地显示、`aria-invalid` 标记、焦点移到首个失败字段，URL 错误会展开可选设置。
2. 身份确认：发现两个同名候选且保持独立；选择无 Lantern 关联的候选时拒绝启动脚本研究。
3. 可观察运行：逐步展示读取与核对步骤；暂停清空定时器，继续从当前步骤恢复，全程只有一个定时器。
4. 新建研究：清空消息、错误、运行定时器、报告、来源选择、重试状态与筛选，把来源与脚本状态复位。
5. 导出：报告存在前「下载 Markdown」保持禁用并在函数内再次拦截；导出使用本地 `Blob`，包含合成声明、问题、结论、来源状态与限制，不写任何百分比可信度。

## 撤回依赖模型

来源状态与结论依赖集中在 `index.html` 的 `sources` 与 `statementDefs` 中，UI 与导出共用同一份判定：

- S1 本人自述、S2 实际作品、S3 第三方观察为有效合成来源；S4 无法访问，始终排除在有效支持之外。
- 撤下 S2 后，所有依赖 S2 的展示与导出同步变为「待复核 · S2 已撤下」：
  - 已支持事实中的界面分区事实
  - 分析推断与机制结论
  - 2024-11 时间线事件
  - 此前与当前给出的脚本回答（依赖 S2 的消息追加待复核标记）
- 仅依赖 S1 / S3 的结论不被误标；恢复 S2 后状态一致恢复。
- 导出在每条受影响语句前直接写入 `**[待复核 · S2 已撤下]**`，不会把受影响语句留在「已支持事实」下不加限定。

## 来源检查器与抽屉

- 桌面检查器提供来源筛选（全部 / 本人 / 作品 / 第三方 / 异常）、短引文、日期、定位、支持、限制与状态；S2 可模拟撤下与恢复。
- 筛选无结果时显示空状态；撤下 S2 后「作品」筛选为空。
- 关闭状态的抽屉带 `hidden` 与 `aria-hidden`，不可聚焦；打开后锁定 Tab/Shift+Tab、`Escape` 关闭并把焦点还给触发控件，背景加 `inert`。
- 首页引用在任意宽度打开来源抽屉；App 桌面引用展开右侧检查器，窄屏打开抽屉。视口变化会收起抽屉并同步检查器，避免控件被搁置。

## 可访问性与降级

- 所有交互有可见键盘焦点；正文对比度按 4.5:1 选择；状态同时用文字与边框形状表达，不只依赖颜色。
- 对话与抽屉使用 `role="dialog"` / `aria-modal`；列表使用 `listbox` / `option` 语义。
- `prefers-reduced-motion` 下停止脚本步骤位移动画；脚本仍会完成。
- 无远程字体、远程脚本、图片或网络请求；所有来源 URL 均为合成标识，原型不导航到伪网站。

## 原型真实性边界

- 只有本地 HTML/CSS/JS：没有真实采集、检索、模型推理、流式事件、持久化或后台服务。
- 研究对象林舟、项目 Lantern 与 S1–S4 均为合成材料；不抓取 `example.org` 等 fixture 域名。
- 运行状态是脚本演示，不伪造实时速度、用户量、覆盖率或任何指标。
- 预设回答与「待复核」标记只演示状态传播与依赖撤回，不构成研究方法结论。
- 正式实现必须读取同一 canonical report；本原型不是第二份报告事实来源。

## 本地验证

在仓库根目录：

```bash
python3 scripts/check_design.py
npm --prefix design/web/qa test
```

`check_design.py` 是静态一致性检查；`prototype.test.cjs` 使用 jsdom 在离线环境驱动原型脚本，覆盖校验、撤回、导出门禁、抽屉焦点与新建研究复位。两者不测量网络、模型或 provider 行为；真实浏览器结果另见 [验证记录](VALIDATION.md)。
