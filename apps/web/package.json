{
  "name": "@stripsearch/web",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "StripSearch authenticated web alpha: same-origin Express + Better Auth + SQLite + vanilla TypeScript client.",
  "engines": {
    "node": "22.23.2"
  },
  "scripts": {
    "test": "node --import tsx --test src/tests/*.test.ts",
    "typecheck": "tsc -p tsconfig.server.json --noEmit && tsc -p tsconfig.client.json --noEmit && tsc -p tsconfig.test.json --noEmit",
    "build:client": "vite build",
    "build:server": "tsc -p tsconfig.server.json",
    "build": "npm run build:client && npm run build:server",
    "start": "node --env-file-if-exists=.env dist/server/index.js",
    "dev": "npm run build:client && node --import tsx --env-file-if-exists=.env --watch src/server/index.ts",
    "dev:client": "vite build --watch"
  },
  "dependencies": {
    "better-auth": "1.7.5",
    "better-call": "1.4.0",
    "better-sqlite3": "13.0.3",
    "express": "5.2.1"
  },
  "devDependencies": {
    "@types/better-sqlite3": "9.6.0",
    "@types/express": "5.0.6",
    "@types/node": "22.20.4",
    "jsdom": "30.1.0",
    "tsx": "4.23.15",
    "typescript": "5.9.3",
    "vite": "8.3.0"
  },
  "allowScripts": {
    "better-sqlite3@13.0.3": true,
    "esbuild@0.28.2": true
  }
}
